{
	"name": "tamed-express-server",
	"version": "3.0.2",
	"description": "A quick express server that converts a js file to express routes, using the js file's exported functions.",
	"main": "tamed-express-server.js",
	"repository": "https://github.com/mehmetkaplan/tamed-express-server",
	"author": "Mehmet Kaplan",
	"license": "MIT",
	"private": false,
	"scripts": {
		"test": "reset && jest --runInBand --coverage --no-cache --forceExit "
	},
	"dependencies": {
		"body-parser": "^1.20.1",
		"cors": "^2.8.5",
		"express": "^4.18.2",
		"fs": "^0.0.1-security",
		"https": "^1.0.0",
		"morgan": "^1.10.0",
		"path": "^0.12.7",
		"tick-log": "^1.1.4"
	},
	"devDependencies": {
		"babel-cli": "^6.26.0",
		"babel-preset-env": "^1.7.0",
		"jest": "^29.4.2",
		"superagent": "^8.0.9",
		"supertest": "^6.3.3"
	},
	"keywords": [
		"express",
		"function name to route",
		"js to express",
		"js to express routes"
	]
}
